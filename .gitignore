node_modules
.next
out
dist

.env
.env*
!.env.example

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.DS_Store
*.pem
*.tsbuildinfo

.worktrees

# Playwright
playwright-report/
test-results/
tests/e2e/.auth/user.json

# Graphify local/runtime files (os outputs publicados — graph.json, graph.html,
# GRAPH_REPORT.md e manifest.json — são não-ocultos e seguem versionados;
# os runtime são dotfiles/dirs)
graphify-out/.*
graphify-out/cache/
graphify-out/cost.json
graphify-out/memory/
graphify-out/reflections/
graphify-out/20*/

# Cobertura de testes (gerada por `npm run test:coverage`)
/coverage

# Ferramentas de IA / assistentes (config e artefatos locais, não versionados)
.agents/
.claude/
.codex/
.impeccable/

# Override local desta máquina (host do banco no compose)
docker-compose.override.yml
